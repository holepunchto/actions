{
  "name": "upload-directory-artifacts",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/artifact": "^2.3.2",
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
